# Cross-platform parity matrix — Alhrajplus Premium

هذه المصفوفة تفرق بين **وجود الميزة في الكود** وبين اعتمادها على تكامل خارجي أو اختبار جهاز فعلي. لا تُعد الميزة مكتملة إنتاجيًا إلا إذا كان المسار موجودًا، والـ API متوافقًا، والـ build ناجحًا، وتم اختبار الحالات الأساسية.

| Feature / workflow | Web | React Native | API مشتركة | الحالة بعد هذه الدفعة |
|---|---:|---:|---:|---|
| تسجيل الدخول والتسجيل والتحقق والبريد | نعم | نعم | نعم | موحد وظيفيًا |
| الرئيسية والفئات والبحث والخريطة | نعم | نعم | نعم | موحد مع اختلاف UX الأصلي |
| إنشاء الإعلان متعدد الخطوات | نعم | نعم | نعم | يحتاج device matrix للحقول المتخصصة |
| صور وفيديو ورفع signed | نعم | نعم جزئيًا | نعم | موحد، مع اختلاف progress UX |
| 360 image sequence | نعم | نعم | نعم | 8–24 صورة، و12–24 موصى بها |
| GLB/GLTF 3D | نعم | نعم عبر WebView/model-viewer | raw Cloudinary | موجود على الطرفين؛ يحتاج اختبار جهاز فعلي وCDN/شبكة |
| الإعجاب والمفضلة والمشاهدة والتعليقات | نعم | نعم | نعم | موحد |
| المتابعة وTrust Graph وملف البائع | نعم | نعم | نعم | Storefront الويب أوسع؛ SellerProfile الموبايل يعرض الهوية والكتالوج والتقييم |
| العروض والعروض المضادة والصلاحية | نعم | نعم بعد إضافة OffersScreen | نعم | موحد في المسار الأساسي |
| المحادثة realtime وoffline outbox | نعم | نعم | نعم/Redis | موحد، يحتاج ضغط staging |
| الإشعارات والقائمة والـ deep links | dropdown + NotificationsPage | NotificationsScreen | نعم | تم توحيد شاشة القائمة؛ push cold-start يحتاج أجهزة فعلية |
| إعدادات Web Push / Expo / HMS | Web Push | Expo/HMS حسب المنصة | نعم | اختلاف platform-specific مقصود |
| الإحالات والـ referral ledger | ProfilePage | ProfileScreen | نعم | موحد في العرض الأساسي |
| المحفظة والإعلانات/الصفقات/المزادات/الطيران | صفحات ويب | شاشات موبايل | نعم | موجود على الطرفين، تكامل الدفع أو الحجز خارجي |
| المساعد الذكي | FAB/Panel | AIAssistantScreen | نعم | UX مختلف لكن capability مشتركة |
| لوحة الإدارة | نعم | لا | admin API | Web-only مقصود أمنيًا؛ تطبيق Admin أصلي ليس مطلوبًا للمستخدم العام |
| Retention والحذف الجماعي وتنظيف Cloudinary | نعم | لا | admin API | Admin web-only مقصود |

## ميزات السوق التي ما زالت خارج parity الكامل

تبقى بعض القدرات الكبيرة خارج هذه الدفعة لأنها تحتاج مزودًا أو قرارًا تشغيليًا: دفع وescrow، فحص VIN مدفوع، تقييم عقاري خارجي، تكامل ATS للوظائف، مكالمات صوتية/فيديو، توصيل وشحن، KYC حكومي، ونظام توصيات ranking مدعوم ببيانات إنتاج. هذه ليست أزرارًا واجهة فقط؛ تحتاج مفاتيح ومزودين وسياسات وعمليات مراجعة.

## التحقق المنفذ

تمت إضافة `OffersScreen` في React Native مع قبول ورفض وcounter-offer وexpiry، وإضافة `NotificationsPage` كاملة على الويب مع mark-one/read-all وdeep links والتحديث من realtime events. نجح Expo export بعد إضافة شاشة العروض، ونجح Web production build بعد إضافة صفحة الإشعارات.

### آخر تحديث

أضيف تفعيل 360 والتحقق من 8–24 صورة داخل `mobile/src/screens/PostScreen.js` أيضًا، فأصبح مسار إنشاء الإعلان على الويب والموبايل يرفض sequence غير الصالح قبل الإرسال، مع توصية 12–24 صورة.

أضيف الآن عارض GLB/GLTF للموبايل عبر `react-native-webview` مع camera-controls وauto-rotate وAR modes عند دعم الجهاز، ويظهر زر 3D في ListingDetail عند وجود `model_3d_url`. يعتمد التشغيل على تحميل model-viewer CDN، لذلك يحتاج اختبار جهاز فعلي واتصال شبكة قبل اعتماد AR/3D النهائي.
