# Phase 1 Baseline Audit — Alhrajplus

## Snapshot

تم تنفيذ الجرد على commit `b13fbeb`، مع وجود ملف مصفوفة المتطلبات غير متتبع بعد باسم `requirements_traceability_pasted_12.md`. الجرد الأولي وجد 148 سطرًا لمسارات Web/Mobile، و348 نتيجة مرتبطة بعبارات mock/demo/placeholder/alerts أو حالات قريبة منها، و368 موضعًا يحتوي على Emoji أو رموز مرشحة للاستبدال. هذه الأرقام ليست أحكامًا نهائية؛ لأنها تشمل رسائل خطأ صحيحة، وترجمات، وتعليقات، وmetadata، ولذلك ستُصنف في المرحلة الثانية إلى إنتاجي، مقبول، أو يحتاج تعديل.

## Findings that require immediate attention

1. `frontend/src/pages/ReelsPage.js` ما زال يعرض حالة `القصص قريباً`، وهو تعارض مباشر مع طلب تشغيل القصص/الفيديوهات فعليًا.
2. `frontend/src/pages/WalletPage.js` يعرض `قريباً عبر بوابة الدفع`، لذلك يجب تحديد هل الدفع خارج النطاق حاليًا أم يلزم ربط حقيقي؛ لا يجوز إبقاء CTA مضللًا.
3. `frontend/src/pages/DownloadPage.js` و`ProfilePage.js` يحتويان حالة غياب رابط المتجر. هذا مقبول فقط إذا كانت الحالة واضحة ومترجمة، ويجب التحقق أن الروابط تُمرر فعلًا من Render/EAS.
4. توجد Emoji في رسائل النجاح والخطأ، badges، أزرار، العناوين، والخريطة. سيتم عمل inventory حسب الملف ثم استبدال العناصر البصرية بـLucide/SVG؛ أما Emoji النصية داخل رسائل المستخدم فستُراجع حسب السياق.
5. توجد استدعاءات `alert()` كثيرة. سيتم تقسيمها إلى رسائل نظام صحيحة، ورسائل UI يجب تحويلها إلى Toast/Dialog موحد Premium، ورسائل فيها نص عربي ثابت يجب تمريرها عبر i18n.
6. يوجد backend seed metadata للفئات والحقول، ويجب عدم حذفه عشوائيًا. سيتم التفريق بين metadata اللازمة للنشر وبين demo marketplace records.
7. نتائج الجرد الأولية تشير إلى أن الاختبار الحقيقي يجب أن يغطي: المزادات، الفيديوهات، الصفقات، الطيران، chat، push، Cloudinary cleanup، Redis، profile phone، country fallback، والترجمات.

## Execution rule

لا يُغلق أي بند من مصفوفة المتطلبات بمجرد تعديل سطر أو نجاح build. يجب أن تكون لكل ميزة: مصدر بيانات، حالات loading/error/empty، Web/Mobile parity، ترجمة، responsive behavior، ثم اختبار وظيفي أو smoke test موثق.

## Next phase

المرحلة التالية هي تصنيف نتائج الجرد إلى ملفات ومكونات ومسارات، ثم بناء قائمة تنفيذ مرتبة حسب الخطورة: أولًا البيانات الوهمية والميزات غير الحقيقية، ثم البنية المشتركة للثيم والترجمة والتنقل، ثم الشاشات الكبيرة، ثم التفاصيل والاختبارات.
