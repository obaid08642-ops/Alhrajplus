// Runtime configuration. Edit this file on the host WITHOUT rebuilding the
// React bundle to point the frontend at a different backend.
// Set BACKEND_URL to your live API host (no trailing slash). Leave empty to
// fall back to the build-time REACT_APP_BACKEND_URL.
window.__APP_CONFIG__ = {
    BACKEND_URL: ""
};
